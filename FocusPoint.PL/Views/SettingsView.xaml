<UserControl x:Class="FocusPoint.PL.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit" xmlns:viewmodels="clr-namespace:FocusPoint.PL.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:SettingsViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Focus Interval (minutes):" Grid.Row="0" Margin="5"/>
        <xctk:IntegerUpDown Value="{Binding CurrentUser.UserSetting.FocusInterval, UpdateSourceTrigger=PropertyChanged}"
                            Minimum="10" Maximum="180" Increment="5"
                            Grid.Row="0" Margin="150,5,0,5" Width="100" HorizontalAlignment="Left"/>

        <TextBlock Text="Number of Work Blocks:" Grid.Row="1" Margin="5"/>
        <xctk:IntegerUpDown Value="{Binding CurrentUser.UserSetting.WorkBlocks, UpdateSourceTrigger=PropertyChanged}"
                            Minimum="1" Maximum="5"
                            Grid.Row="1" Margin="150,5,0,5" Width="100" HorizontalAlignment="Left"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="5">
            <TextBlock Text="Use Internal Timer:" VerticalAlignment="Center"/>
            <CheckBox IsChecked="{Binding CurrentUser.UserSetting.UseInternalTimer, UpdateSourceTrigger=PropertyChanged}" 
                      Margin="10,0,0,0"/>
        </StackPanel>

        <Button Content="Save Settings" Grid.Row="3" Margin="0,10,0,0" Padding="5"
                HorizontalAlignment="Center"
                Command="{Binding SaveSettingsCommand}"/>
    </Grid>
</UserControl>